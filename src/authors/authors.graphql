type Author {
  id: Int!
  firstName: String!
  lastName: String
  phoneNumber: String
  address: String
}

type Query {
  author(id: Int!): Author
}

type Mutation {
  editPhoneNumber(id: Int!, phoneNumber: String!): Author
}
